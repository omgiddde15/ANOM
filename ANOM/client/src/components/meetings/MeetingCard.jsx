export default function MeetingCard({ meeting, partnerName, isInvitee, onAccept, onReject, onCancel }) {
  const styles = { pending: 'bg-amber-100 text-amber-800', accepted: 'bg-emerald-100 text-emerald-800', rejected: 'bg-rose-100 text-rose-800' };
  return <article className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex items-start justify-between gap-3"><div><p className="font-bold text-slate-900">{partnerName || 'Your match'}</p><p className="mt-1 text-sm text-slate-600">{meeting.date} at {meeting.time}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold capitalize ${styles[meeting.status] || 'bg-slate-100 text-slate-700'}`}>{meeting.status}</span></div><p className="mt-3 text-sm text-slate-700">📍 {meeting.venue}</p>{meeting.status === 'pending' && <div className="mt-4 flex flex-wrap gap-2">{isInvitee && <><button onClick={() => onAccept(meeting.id)} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-semibold text-white hover:bg-emerald-700">Accept</button><button onClick={() => onReject(meeting.id)} className="rounded-lg border border-rose-200 px-3 py-2 text-xs font-semibold text-rose-700 hover:bg-rose-50">Reject</button></>}<button onClick={() => onCancel(meeting.id)} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-600 hover:bg-slate-50">Cancel</button></div>}</article>;
}
