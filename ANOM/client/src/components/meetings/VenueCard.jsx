export default function VenueCard({ venue, onSelect }) {
  const mapUrl = venue.googleMapsUrl || `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${venue.name} ${venue.address || ''}`)}`;
  return <article className="rounded-2xl bg-white p-4 shadow-sm ring-1 ring-slate-200"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold text-slate-900">📍 {venue.name}</h3><span className="mt-1 inline-block rounded-full bg-indigo-50 px-2 py-1 text-xs font-semibold text-indigo-700">{venue.type}</span></div><a href={mapUrl} target="_blank" rel="noreferrer" className="text-xs font-semibold text-indigo-600 hover:underline">Open in Maps ↗</a></div><p className="mt-3 text-sm text-slate-600">{venue.reason}</p><p className="mt-2 text-sm text-slate-500">{venue.address}</p><button onClick={() => onSelect(venue)} className="mt-4 rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white hover:bg-indigo-700">Select venue</button></article>;
}
