const { Router }=require('express'); const multer=require('multer'); const {verifyToken}=require('../middleware/auth'); const {uploadProfilePhotoHandler,deleteProfilePhotoHandler}=require('../controllers/uploadController');
const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024},fileFilter:(_r,file,cb)=>cb(null,['image/jpeg','image/png','image/webp'].includes(file.mimetype))}); const router=Router(); router.use(verifyToken); router.post('/profile-photo',upload.single('photo'),uploadProfilePhotoHandler);router.delete('/profile-photo',deleteProfilePhotoHandler);router.use((err,_req,res,_next)=>res.status(err.code==='LIMIT_FILE_SIZE'?413:422).json({success:false,message:err.code==='LIMIT_FILE_SIZE'?'Image must be 5MB or smaller.':'Only JPG, PNG, and WebP images are allowed.'}));module.exports=router;
